# Excluded from the Maestro build context.
# Keep this in sync with the COPY directives in Dockerfile.
#
# Included (COPYd):   app/  tests/  scripts/  tools/  alembic/
#                     stori_tourdeforce/  reference_midi/
#                     alembic.ini  pyproject.toml  mvp_happy_path.py  entrypoint.sh
# Excluded (below):   everything else

.git
.github

# Secrets — never in the image
.env
.env.*
!.env.example

# External service (separate build context)
storpheus/

# Infrastructure / deploy configs (not needed at runtime)
deploy/
docs/
assets/

# Dev artifacts
**/__pycache__/
**/.pytest_cache/
**/.mypy_cache/
**/*.pyc
**/*.pyo
.venv/
venv/

# Misc
docker-compose*.yml
*.md
QUICK_TEST_PROMPTS.txt
LICENSE
