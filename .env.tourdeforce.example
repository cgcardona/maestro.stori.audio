# Tour de Force environment variables
# Copy to .env.tourdeforce and fill in your JWT:
#   cp .env.tourdeforce.example .env.tourdeforce
#
# NEVER commit .env.tourdeforce — it is in .gitignore.

STORI_JWT=your-jwt-token-here
